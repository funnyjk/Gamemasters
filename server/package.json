{
  "name": "gamemasters-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "prisma deploy --prefix database && prisma generate --prefix database",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-datasource-rest": "^0.6.11",
    "prisma": "^1.34.10"
  }
}
